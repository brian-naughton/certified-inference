# FIREWALL: internal working docs — never tracked, never in history, never pushed.
# Specs, plans, adversarial reviews, positioning and outreach notes live on disk only.
.superpowers/
docs/superpowers/
docs/reviews/
docs/plans/
docs/specs/
INTERNAL*
OUTREACH*
REVIEW-BRIEF*
*.internal.md
sol-prompt*
*-draft.txt
x-post-*

data/
__pycache__/
*.egg-info/
.pytest_cache/
.DS_Store
# Task 0.4: the GPT-2 hex export is ~2.1 GB (>> GitHub's 100 MB hard limit) —
# regenerate on demand; see docs/PROVENANCE.md "Weight hex export".
certificates/gpt2-small.weights.json
